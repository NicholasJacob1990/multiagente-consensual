#!/bin/sh
set -eu

BRIDGE_BIN_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
BRIDGE_PLUGIN_ROOT=$(CDPATH= cd -- "$BRIDGE_BIN_DIR/.." && pwd)
export MULTIAGENT_PLUGIN_ROOT="$BRIDGE_PLUGIN_ROOT"
exec python3 "$BRIDGE_PLUGIN_ROOT/scripts/cowork_bridge.py" "$@"
